<?xml version="1.0"?>
<ruleset name="Custom Coding Standard based on Doctrine">
    <arg name="basepath" value="."/>
    <arg name="extensions" value="php"/>
    <arg name="parallel" value="80"/>
    <arg name="cache" value=".php_cs.cache"/>
    <arg name="colors"/>

    <!-- Ignore warnings, show progress of the run and show sniff names -->
    <arg value="ps"/>

    <!-- Paths to check -->
    <file>src</file>
    <file>test</file>
    <exclude-pattern>test/config/config.php</exclude-pattern>

    <!-- Inherit rules from Doctrine Coding Standard -->
    <rule ref="Doctrine" />

    <!-- Disable native type hint whilst this lib is compatible with 7.3 -->
    <rule ref="SlevomatCodingStandard.TypeHints.PropertyTypeHint">
        <properties>
            <property name="enableNativeTypeHint" value="false" />
        </properties>
    </rule>

    <rule ref="Generic.Formatting.MultipleStatementAlignment.NotSame">
        <severity>0</severity>
    </rule>

    <rule ref="Generic.Files.LineLength.TooLong">
        <exclude-pattern>/test/</exclude-pattern>
    </rule>
</ruleset>
